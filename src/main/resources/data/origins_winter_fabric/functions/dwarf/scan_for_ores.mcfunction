# Сканирование железной руды
execute positioned ~-10 ~-10 ~-10 run function origins_winter_fabric:dwarf/ore_scan_loop

# ВНИМАНИЕ: Функция ниже должна быть создана как отдельный файл
# в папке functions/dwarf/ вашего датапака